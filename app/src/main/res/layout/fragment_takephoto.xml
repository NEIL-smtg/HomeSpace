<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="30sp"
        android:text="OPEN CAMERA"
        android:layout_marginTop="40dp"
        android:layout_marginRight="30dp"
        android:layout_marginLeft="30dp"
        android:background="@drawable/white_round"
        android:id="@+id/btnLaunchCamera"
        />

    <ImageView
        android:id="@+id/tmpProfilePic"
        android:layout_width="270dp"
        android:layout_height="270dp"
        android:layout_below="@+id/btnLaunchCamera"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tmpProfilePic"
        android:layout_alignParentRight="true"
        android:layout_marginRight="20dp"
        android:textSize="20sp"
        android:text="NEXT"
        android:textColor="@color/lightBlue"
        android:clickable="true"
        android:layout_marginTop="20dp"
        android:visibility="gone"
        android:id="@+id/takephoto_next"
        />

    <ProgressBar
        android:id="@+id/camera_progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/takephoto_next"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:visibility="gone"
        />


</RelativeLayout>